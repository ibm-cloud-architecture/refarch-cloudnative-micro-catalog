# Server configuration
server:
  context-path: /micro
  port: 8081

# Spring properties
spring:
  application:
     name: catalog-microservice

# Elasticsearch
elasticsearch:
  url: http://127.0.0.1:9200
  user:
  password:
  index: micro
  doc_type: items

inventoryService:
  url: http://127.0.0.1:8080
